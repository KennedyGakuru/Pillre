import {View, Text} from 'react-native';

const ProfileScreen = () : JSX.Element => {
    return(
        <View className="flex-1 justify-center items-center bg-[#EA4335]">
            <Text className="text-bold text-[24px] ">Profile Screen</Text>
        </View>
    );
};

export default ProfileScreen;