import {View, Text} from 'react-native';

const HomeScreen = () : JSX.Element => {
    return(
        <View className=" flex-1 bg-[#29B6F6] items-center justify-center">
            <Text className="text bold">Pillre</Text>
        </View>
    );
};

export default HomeScreen;