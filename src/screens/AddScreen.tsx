import {View, Text} from 'react-native';

const AddScreen = (): JSX.Element => {
    return(
        <View className="flex-1 items-center justify-center bg-[#34A853]">
            <Text>Add Medicine</Text>
        </View>
    );
};

export default AddScreen;